services:
  app:
    build: .
    ports:
      - "8501:8501"
    env_file:
      - .env
    environment:
      DATABASE_URL: sqlite:////data/nischen_explorer.db
    volumes:
      - app_data:/data
    restart: unless-stopped
volumes:
  app_data:
